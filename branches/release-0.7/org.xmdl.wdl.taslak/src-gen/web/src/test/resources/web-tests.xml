<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project basedir="." default="run-all-tests">
    <!-- Use i18n bundle, 2nd one listed is the default -->
    <property file="\${basedir}/target/${webapp.name}/WEB-INF/classes/ApplicationResources_${user.language}.properties"/>
    <property file="\${basedir}/target/${webapp.name}/WEB-INF/classes/ApplicationResources.properties"/>

    <!-- runs all targets -->
    <target depends="ApplicationTests" description="Call and executes all test cases (targets)" name="run-all-tests"/>

    <!-- application specific tests grouped by each entity -->
    <target depends="OrderTests,SupplierTests,ProductTests,OrderElementTests,ProfileTests" description="Calls all application tests" name="ApplicationTests">
    </target>

    <!--Order-START-->
    <!-- runs order-related tests -->
    <target depends="SearchOrders,EditOrder,SaveOrder,AddOrder,                     CopyOrder,DeleteOrder,MassDeleteOrder" description="Call and executes all order test cases (targets)" name="OrderTests">
        <echo>Successfully ran all Order UI tests!</echo>
    </target>

    <!-- Verify the orders list screen displays without errors -->
    <target description="Tests search for and displaying all orders" name="SearchOrders">
        <webtest name="searchOrders">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click View Order link" url="/Order/orders.do"/>
                <verifytitle description="we should see the orderList title" regex="true" text=".*${orderList.title}.*"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the edit order screen displays without errors -->
    <target description="Tests editing an existing Order's information" name="EditOrder">
        <webtest name="editOrder">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit Order link" url="/Order/editOrder.do?id=1"/>
                <verifytitle description="we should see the orderDetail title" regex="true" text=".*${orderDetail.title}.*"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a order and then save -->
    <target description="Tests editing and saving a order" name="SaveOrder">
        <webtest name="saveOrder">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit Order link" url="/Order/editOrder.do?id=1"/>
                <verifytitle description="we should see the orderDetail title" regex="true" text=".*${orderDetail.title}.*"/>

                <!-- update some of the required fields -->
                <setInputField description="set name" name="order.name" value="EbmK afatU2Xc8 "/>
                <setInputField description="set priceTotals" name="order.priceTotals" value="0.6571071197650238"/>
                <setInputField description="set createDate" name="order.createDate" value="11/17/2002"/>

                <clickbutton description="Click Update" label="${button.update}"/>
                <verifytitle description="Page re-appears if save successful" regex="true" text=".*${orderDetail.title}.*"/>
                <verifytext description="verify success message" text="${order.updated}"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new Order -->
    <target description="Adds a new Order" name="AddOrder">
        <webtest name="addOrder">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Add Button" url="/Order/editOrder.do"/>
                <verifytitle description="we should see the orderDetail title" regex="true" text=".*${orderDetail.title}.*"/>

                <!-- enter required fields -->
                <setInputField description="set name" name="order.name" value="EbmK afatU2Xc8 "/>
                <setInputField description="set priceTotals" name="order.priceTotals" value="0.6571071197650238"/>
                <setInputField description="set createDate" name="order.createDate" value="11/17/2002"/>

                <clickbutton description="Click button 'Add'" label="${button.add}"/>
                <verifytitle description="Order List appears if save successful" regex="true" text=".*${orderList.title}.*"/>
                <verifytext description="verify success message" text="${order.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Copy existing order -->
    <target description="Copies existing Order" name="CopyOrder">
        <webtest name="copyOrder">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Entity link" url="/Order/copyOrder.do?idToCopy=1"/>
                <verifytitle description="Values filled by the properties of the order" regex="true" text=".*${orderDetail.title}.*"/>
            </steps>
        </webtest>
    </target>


    <!-- Delete existing order -->
    <target description="Deletes existing Order" name="DeleteOrder">
        <webtest name="deleteOrder">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit Order link" url="/Order/editOrder.do?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton description="Click button 'Delete'" label="${button.delete}"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Order List" regex="true" text=".*${orderList.title}.*"/>
                <verifytext description="verify success message" text="${order.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Mass Delete existing orders -->
    <target description="Mass Delete Orders" name="MassDeleteOrder">
        <webtest name="massDeleteOrder">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click List Orders link" url="/Order/orders.do"/>
                <setCheckbox description="clicks the select all checkbox" name="allbox"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton description="Click button 'Delete'" label="${button.delete}"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Orders List" regex="true" text=".*${orderList.title}.*"/>
                <verifytext description="verify success message" text="${Order.deleted}"/>
            </steps>
        </webtest>
    </target>
    <!--Order-END-->

    <!--Supplier-START-->
    <!-- runs supplier-related tests -->
    <target depends="SearchSuppliers,EditSupplier,SaveSupplier,AddSupplier,                     CopySupplier,DeleteSupplier,MassDeleteSupplier" description="Call and executes all supplier test cases (targets)" name="SupplierTests">
        <echo>Successfully ran all Supplier UI tests!</echo>
    </target>

    <!-- Verify the suppliers list screen displays without errors -->
    <target description="Tests search for and displaying all suppliers" name="SearchSuppliers">
        <webtest name="searchSuppliers">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click View Supplier link" url="/Supplier/suppliers.do"/>
                <verifytitle description="we should see the supplierList title" regex="true" text=".*${supplierList.title}.*"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the edit supplier screen displays without errors -->
    <target description="Tests editing an existing Supplier's information" name="EditSupplier">
        <webtest name="editSupplier">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit Supplier link" url="/Supplier/editSupplier.do?id=1"/>
                <verifytitle description="we should see the supplierDetail title" regex="true" text=".*${supplierDetail.title}.*"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a supplier and then save -->
    <target description="Tests editing and saving a supplier" name="SaveSupplier">
        <webtest name="saveSupplier">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit Supplier link" url="/Supplier/editSupplier.do?id=1"/>
                <verifytitle description="we should see the supplierDetail title" regex="true" text=".*${supplierDetail.title}.*"/>

                <!-- update some of the required fields -->
                <setInputField description="set name" name="supplier.name" value="hznQcdE f68MD Q"/>

                <clickbutton description="Click Update" label="${button.update}"/>
                <verifytitle description="Page re-appears if save successful" regex="true" text=".*${supplierDetail.title}.*"/>
                <verifytext description="verify success message" text="${supplier.updated}"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new Supplier -->
    <target description="Adds a new Supplier" name="AddSupplier">
        <webtest name="addSupplier">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Add Button" url="/Supplier/editSupplier.do"/>
                <verifytitle description="we should see the supplierDetail title" regex="true" text=".*${supplierDetail.title}.*"/>

                <!-- enter required fields -->
                <setInputField description="set name" name="supplier.name" value="hznQcdE f68MD Q"/>

                <clickbutton description="Click button 'Add'" label="${button.add}"/>
                <verifytitle description="Supplier List appears if save successful" regex="true" text=".*${supplierList.title}.*"/>
                <verifytext description="verify success message" text="${supplier.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Copy existing supplier -->
    <target description="Copies existing Supplier" name="CopySupplier">
        <webtest name="copySupplier">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Entity link" url="/Supplier/copySupplier.do?idToCopy=1"/>
                <verifytitle description="Values filled by the properties of the supplier" regex="true" text=".*${supplierDetail.title}.*"/>
            </steps>
        </webtest>
    </target>


    <!-- Delete existing supplier -->
    <target description="Deletes existing Supplier" name="DeleteSupplier">
        <webtest name="deleteSupplier">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit Supplier link" url="/Supplier/editSupplier.do?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton description="Click button 'Delete'" label="${button.delete}"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Supplier List" regex="true" text=".*${supplierList.title}.*"/>
                <verifytext description="verify success message" text="${supplier.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Mass Delete existing suppliers -->
    <target description="Mass Delete Suppliers" name="MassDeleteSupplier">
        <webtest name="massDeleteSupplier">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click List Suppliers link" url="/Supplier/suppliers.do"/>
                <setCheckbox description="clicks the select all checkbox" name="allbox"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton description="Click button 'Delete'" label="${button.delete}"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Suppliers List" regex="true" text=".*${supplierList.title}.*"/>
                <verifytext description="verify success message" text="${Supplier.deleted}"/>
            </steps>
        </webtest>
    </target>
    <!--Supplier-END-->

    <!--Product-START-->
    <!-- runs product-related tests -->
    <target depends="SearchProducts,EditProduct,SaveProduct,AddProduct,                     CopyProduct,DeleteProduct,MassDeleteProduct" description="Call and executes all product test cases (targets)" name="ProductTests">
        <echo>Successfully ran all Product UI tests!</echo>
    </target>

    <!-- Verify the products list screen displays without errors -->
    <target description="Tests search for and displaying all products" name="SearchProducts">
        <webtest name="searchProducts">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click View Product link" url="/Product/products.do"/>
                <verifytitle description="we should see the productList title" regex="true" text=".*${productList.title}.*"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the edit product screen displays without errors -->
    <target description="Tests editing an existing Product's information" name="EditProduct">
        <webtest name="editProduct">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit Product link" url="/Product/editProduct.do?id=1"/>
                <verifytitle description="we should see the productDetail title" regex="true" text=".*${productDetail.title}.*"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a product and then save -->
    <target description="Tests editing and saving a product" name="SaveProduct">
        <webtest name="saveProduct">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit Product link" url="/Product/editProduct.do?id=1"/>
                <verifytitle description="we should see the productDetail title" regex="true" text=".*${productDetail.title}.*"/>

                <!-- update some of the required fields -->
                <setInputField description="set name" name="product.name" value="qz7Px5kW796 vfj"/>
                <setInputField description="set price" name="product.price" value="null"/>
                <setInputField description="set productType" name="product.productType" value="2"/>

                <clickbutton description="Click Update" label="${button.update}"/>
                <verifytitle description="Page re-appears if save successful" regex="true" text=".*${productDetail.title}.*"/>
                <verifytext description="verify success message" text="${product.updated}"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new Product -->
    <target description="Adds a new Product" name="AddProduct">
        <webtest name="addProduct">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Add Button" url="/Product/editProduct.do"/>
                <verifytitle description="we should see the productDetail title" regex="true" text=".*${productDetail.title}.*"/>

                <!-- enter required fields -->
                <setInputField description="set name" name="product.name" value="qz7Px5kW796 vfj"/>
                <setInputField description="set price" name="product.price" value="null"/>
                <setInputField description="set productType" name="product.productType" value="2"/>

                <clickbutton description="Click button 'Add'" label="${button.add}"/>
                <verifytitle description="Product List appears if save successful" regex="true" text=".*${productList.title}.*"/>
                <verifytext description="verify success message" text="${product.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Copy existing product -->
    <target description="Copies existing Product" name="CopyProduct">
        <webtest name="copyProduct">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Entity link" url="/Product/copyProduct.do?idToCopy=1"/>
                <verifytitle description="Values filled by the properties of the product" regex="true" text=".*${productDetail.title}.*"/>
            </steps>
        </webtest>
    </target>


    <!-- Delete existing product -->
    <target description="Deletes existing Product" name="DeleteProduct">
        <webtest name="deleteProduct">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit Product link" url="/Product/editProduct.do?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton description="Click button 'Delete'" label="${button.delete}"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Product List" regex="true" text=".*${productList.title}.*"/>
                <verifytext description="verify success message" text="${product.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Mass Delete existing products -->
    <target description="Mass Delete Products" name="MassDeleteProduct">
        <webtest name="massDeleteProduct">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click List Products link" url="/Product/products.do"/>
                <setCheckbox description="clicks the select all checkbox" name="allbox"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton description="Click button 'Delete'" label="${button.delete}"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Products List" regex="true" text=".*${productList.title}.*"/>
                <verifytext description="verify success message" text="${Product.deleted}"/>
            </steps>
        </webtest>
    </target>
    <!--Product-END-->

    <!--OrderElement-START-->
    <!-- runs orderElement-related tests -->
    <target depends="SearchOrderElements,EditOrderElement,SaveOrderElement,AddOrderElement,                     CopyOrderElement,DeleteOrderElement,MassDeleteOrderElement" description="Call and executes all orderElement test cases (targets)" name="OrderElementTests">
        <echo>Successfully ran all OrderElement UI tests!</echo>
    </target>

    <!-- Verify the orderElements list screen displays without errors -->
    <target description="Tests search for and displaying all orderElements" name="SearchOrderElements">
        <webtest name="searchOrderElements">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click View OrderElement link" url="/OrderElement/orderElements.do"/>
                <verifytitle description="we should see the orderElementList title" regex="true" text=".*${orderElementList.title}.*"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the edit orderElement screen displays without errors -->
    <target description="Tests editing an existing OrderElement's information" name="EditOrderElement">
        <webtest name="editOrderElement">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit OrderElement link" url="/OrderElement/editOrderElement.do?id=1"/>
                <verifytitle description="we should see the orderElementDetail title" regex="true" text=".*${orderElementDetail.title}.*"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a orderElement and then save -->
    <target description="Tests editing and saving a orderElement" name="SaveOrderElement">
        <webtest name="saveOrderElement">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit OrderElement link" url="/OrderElement/editOrderElement.do?id=1"/>
                <verifytitle description="we should see the orderElementDetail title" regex="true" text=".*${orderElementDetail.title}.*"/>

                <!-- update some of the required fields -->
                <setInputField description="set quantity" name="orderElement.quantity" value="-8244772088778785312"/>

                <clickbutton description="Click Update" label="${button.update}"/>
                <verifytitle description="Page re-appears if save successful" regex="true" text=".*${orderElementDetail.title}.*"/>
                <verifytext description="verify success message" text="${orderElement.updated}"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new OrderElement -->
    <target description="Adds a new OrderElement" name="AddOrderElement">
        <webtest name="addOrderElement">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Add Button" url="/OrderElement/editOrderElement.do"/>
                <verifytitle description="we should see the orderElementDetail title" regex="true" text=".*${orderElementDetail.title}.*"/>

                <!-- enter required fields -->
                <setInputField description="set quantity" name="orderElement.quantity" value="-8244772088778785312"/>

                <clickbutton description="Click button 'Add'" label="${button.add}"/>
                <verifytitle description="OrderElement List appears if save successful" regex="true" text=".*${orderElementList.title}.*"/>
                <verifytext description="verify success message" text="${orderElement.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Copy existing orderElement -->
    <target description="Copies existing OrderElement" name="CopyOrderElement">
        <webtest name="copyOrderElement">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Entity link" url="/OrderElement/copyOrderElement.do?idToCopy=1"/>
                <verifytitle description="Values filled by the properties of the orderElement" regex="true" text=".*${orderElementDetail.title}.*"/>
            </steps>
        </webtest>
    </target>


    <!-- Delete existing orderElement -->
    <target description="Deletes existing OrderElement" name="DeleteOrderElement">
        <webtest name="deleteOrderElement">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit OrderElement link" url="/OrderElement/editOrderElement.do?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton description="Click button 'Delete'" label="${button.delete}"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OrderElement List" regex="true" text=".*${orderElementList.title}.*"/>
                <verifytext description="verify success message" text="${orderElement.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Mass Delete existing orderElements -->
    <target description="Mass Delete OrderElements" name="MassDeleteOrderElement">
        <webtest name="massDeleteOrderElement">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click List OrderElements link" url="/OrderElement/orderElements.do"/>
                <setCheckbox description="clicks the select all checkbox" name="allbox"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton description="Click button 'Delete'" label="${button.delete}"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display OrderElements List" regex="true" text=".*${orderElementList.title}.*"/>
                <verifytext description="verify success message" text="${OrderElement.deleted}"/>
            </steps>
        </webtest>
    </target>
    <!--OrderElement-END-->

    <!--Profile-START-->
    <!-- runs profile-related tests -->
    <target depends="SearchProfiles,EditProfile,SaveProfile,AddProfile,                     CopyProfile,DeleteProfile,MassDeleteProfile" description="Call and executes all profile test cases (targets)" name="ProfileTests">
        <echo>Successfully ran all Profile UI tests!</echo>
    </target>

    <!-- Verify the profiles list screen displays without errors -->
    <target description="Tests search for and displaying all profiles" name="SearchProfiles">
        <webtest name="searchProfiles">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click View Profile link" url="/Profile/profiles.do"/>
                <verifytitle description="we should see the profileList title" regex="true" text=".*${profileList.title}.*"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the edit profile screen displays without errors -->
    <target description="Tests editing an existing Profile's information" name="EditProfile">
        <webtest name="editProfile">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit Profile link" url="/Profile/editProfile.do?id=1"/>
                <verifytitle description="we should see the profileDetail title" regex="true" text=".*${profileDetail.title}.*"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a profile and then save -->
    <target description="Tests editing and saving a profile" name="SaveProfile">
        <webtest name="saveProfile">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit Profile link" url="/Profile/editProfile.do?id=1"/>
                <verifytitle description="we should see the profileDetail title" regex="true" text=".*${profileDetail.title}.*"/>

                <!-- update some of the required fields -->
                <setInputField description="set privatePhone" name="profile.privatePhone" value="jm85sM 2s5mntS "/>

                <clickbutton description="Click Update" label="${button.update}"/>
                <verifytitle description="Page re-appears if save successful" regex="true" text=".*${profileDetail.title}.*"/>
                <verifytext description="verify success message" text="${profile.updated}"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new Profile -->
    <target description="Adds a new Profile" name="AddProfile">
        <webtest name="addProfile">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Add Button" url="/Profile/editProfile.do"/>
                <verifytitle description="we should see the profileDetail title" regex="true" text=".*${profileDetail.title}.*"/>

                <!-- enter required fields -->
                <setInputField description="set privatePhone" name="profile.privatePhone" value="jm85sM 2s5mntS "/>

                <clickbutton description="Click button 'Add'" label="${button.add}"/>
                <verifytitle description="Profile List appears if save successful" regex="true" text=".*${profileList.title}.*"/>
                <verifytext description="verify success message" text="${profile.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Copy existing profile -->
    <target description="Copies existing Profile" name="CopyProfile">
        <webtest name="copyProfile">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Entity link" url="/Profile/copyProfile.do?idToCopy=1"/>
                <verifytitle description="Values filled by the properties of the profile" regex="true" text=".*${profileDetail.title}.*"/>
            </steps>
        </webtest>
    </target>


    <!-- Delete existing profile -->
    <target description="Deletes existing Profile" name="DeleteProfile">
        <webtest name="deleteProfile">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click Edit Profile link" url="/Profile/editProfile.do?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton description="Click button 'Delete'" label="${button.delete}"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Profile List" regex="true" text=".*${profileList.title}.*"/>
                <verifytext description="verify success message" text="${profile.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Mass Delete existing profiles -->
    <target description="Mass Delete Profiles" name="MassDeleteProfile">
        <webtest name="massDeleteProfile">
			<config basepath="${webapp.name}" host="${host}" port="${port}" protocol="http" resultfile="web-tests-result.xml" resultpath="target/webtest-data" saveresponse="true" summary="true">
			    <header name="Accept-Language" value="${user.language}"/>
			</config>
            <steps>
				<sequential>
				    <invoke description="get Login Page" url="/"/>
				    <verifytitle description="we should see the login title" regex="true" text=".*${login.title}.*"/>
				    <setinputfield description="set user name" name="j_username" value="admin"/>
				    <setinputfield description="set password" name="j_password" value="admin"/>
				    <clickbutton description="Click the submit button" label="${button.login}"/>
				    <verifytitle description="Home Page follows if login ok" regex="true" text=".*${mainMenu.title}.*"/>
				</sequential>
                <invoke description="click List Profiles link" url="/Profile/profiles.do"/>
                <setCheckbox description="clicks the select all checkbox" name="allbox"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton description="Click button 'Delete'" label="${button.delete}"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Profiles List" regex="true" text=".*${profileList.title}.*"/>
                <verifytext description="verify success message" text="${Profile.deleted}"/>
            </steps>
        </webtest>
    </target>
    <!--Profile-END-->


</project>
